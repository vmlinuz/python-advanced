"""
1. Високонавантажений JSON-інжест

Використати streaming JSON parsing без повного завантаження подій у памʼять.
Зчитувати дані невеликими буферами через reusable bytearray + memoryview, щоб уникнути зайвих алокацій.
Парсити події інкрементально за допомогою швидкого JSON-парсера (наприклад, SIMD-підходи).
Формувати батчі в межах одного буфера й одразу вивантажувати на диск.
Це мінімізує копії, зменшує тиск на GC і дає можливість тримати пам’ять стабільною при великих обʼємах.
Можливі інструменти для обраної концепції:
        •	simdjson (швидкий JSON-парсер із мінімальними копіями);
        •	ijson (streaming-парсер);
        •	memoryview, bytearray для zero-copy читання;
        •	aiofiles або raw socket reading для потокового прийому.
"""

"""
2. API повертає великий CSV

Використати zero-copy передачу файлів через механізми файлової системи, наприклад передачу даних напряму
з файла у сокет (OS-level sendfile).
Відправляти файл у потоковому режимі без зчитування в Python-обʼєкти.
Це усуває копіювання, забезпечує мінімальний RSS та гарантовано вписується у RAM-ліміт.
Можливі інструменти для обраної концепції:
	•	os.sendfile;
	•	starlette.responses.FileResponse (використовує sendfile всередині);
	•	aiofiles -> chunked transfer для fallback;
	•	uvicorn/gunicorn з підтримкою zero-copy.
"""

"""
3. Обробка WAV-файлів (20–50 MB)

Працювати з аудіо in-place, створюючи представлення буфера через memoryview, array, або np.frombuffer (zero-copy).
Обробка sample’ів має виконуватися без створення нових масивів - лише масштабування значень у тому ж буфері.
Таким чином, обробка не створює додаткових копій та не перевантажує памʼять.
Можливі інструменти для обраної концепції:
	•	np.frombuffer -> NumPy in-place операції;
	•	memoryview + array('h');
	•	scipy (обережно, але може працювати in-place);
	•	ffmpeg через pipe (обробка поза Python heap).
"""

"""
4. Аналіз великого масиву чисел у API

Використати векторизацію та колонкові структури даних.
Завантажити масив у NumPy/Arrow без створення проміжних Python-обʼєктів.
Усі обчислення виконати у вигляді операцій над безперервними блоками памʼяті, які працюють значно швидше
та не створюють зайвих копій.
Це різко зменшує overhead і значно прискорює обробку.
Можливі інструменти для обраної концепції:
	•	NumPy;
	•	PyArrow arrays;
	•	Numba (для прискорених циклів над масивами);
	•	Polars (за потреби побудувати складніший пайплайн).
"""

"""
5. Парсинг лог-файлу 2–5 GB

Використати memory-mapped I/O (mmap) або потокове читання файла невеликими порціями.
Операції пошуку виконувати без створення проміжних рядкових копій, працюючи безпосередньо поверх mmap buffer.
Це дозволить читати файл дуже швидко, не завантажуючи його у RAM.
Можливі інструменти для обраної концепції:
	•	модуль mmap;
	•	pathlib.Path.open() з лінійним читанням;
	•	re для пошуку по memoryview;
	•	aiofiles для асинхронної обробки великих файлів.
"""

"""
6. Генерація великих HTML документів при високому RPS

Використати буферизовану генерацію: побудову HTML через масиви байтів або списки фрагментів,
які об’єднуються один раз наприкінці.
Це уникає сотень дрібних алокацій та зменшує фрагментацію, забезпечуючи стабільну роботу при високому RPS.
Можливі інструменти для обраної концепції:
	•	io.StringIO або io.BytesIO;
	•	шаблонізатори, що працюють зі стрімами (Jinja2 environment streaming);
	•	bytearray для побудови великих HTML;
	•	HTML builder libraries зі стрімінговим інтерфейсом.
"""

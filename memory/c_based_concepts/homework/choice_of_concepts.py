"""
Завдання:
    Для кожної ситуації нижче необхідно обрати оптимальну стратегію роботи
    з пам’яттю, зважаючи на обмеження середовища, вимоги продуктивності та обмеження клієнта.
    Найголовніше - визначити саме стратегію. Після зʼясування підходу можна пошукати і інструменти
    цієї стратегії, що допоможуть у розв'язання задачі.

    Для кожного кейсу потрібно:
        1) Назвати обрану стратегію(ї).
        2) Коротко пояснити, чому саме така стратегія оптимальна в умовах задачі.

Актуальність:
    Реальні системи часто працюють у середовищах з малим обсягом RAM, великими файлами,
    високим навантаженням або обмеженнями інфраструктури. Уміння обирати правильний
    підхід до читання, парсингу та передачі даних дозволяє уникнути переповнення памʼяті,
    зменшити кількість копій, знизити тиск на GC і стабілізувати продуктивність, а також
    зменшити ризики майбутніх проблем на кшталт фрагментації. Це критично важливо для
    бекенд-сервісів, що працюють з великими JSON, CSV, стрімами подій або файловими API.
"""

"""
1. Високонавантажений JSON-інжест

Сервіс отримує 50–70 тис. JSON-подій щосекунди.
Повідомлення великі, але їх структура повторювана: ключі переважно однакові.
Потрібно ефективно та швидко обробляти події.
Обмеження середовища: 2 vCPU, RAM < 1 GB.
Формат JSON змінювати не можна - це вимога клієнта.

Питання:
Який підхід до роботи з памʼяттю дозволить досягнути стабільної продуктивності без переповнення RAM?

Відповідь:
    Стратегія: інтернування рядків (string interning) + потоковий парсинг (streaming parsing).

    Оскільки JSON-ключі повторюються у десятках тисяч повідомлень, кожен розпарсений dict
    створює нові str-обʼєкти для тих самих ключів, що швидко вичерпує < 1 GB RAM.
    Використання sys.intern() (або бібліотеки orjson, яка інтернує ключі автоматично на рівні C)
    дозволяє зберігати лише один екземпляр кожного ключа і перевикористовувати його між усіма
    подіями — це різко зменшує кількість алокацій і тиск на GC.

    Потоковий парсинг (наприклад, ijson для SAX-подібної обробки, або порційне читання з orjson)
    гарантує, що в памʼяті одночасно перебуває лише одна-дві події, а не весь вхідний потік.

    Інструменти: orjson (C-парсер із вбудованим interning), ijson (SAX-стримінг), sys.intern().
"""

"""
2. API повертає великий CSV

API endpoint /export має повертати користувачам CSV-файл розміром до 300–500 MB.
Поточна реалізація читає файл у памʼять повністю перед відправкою.
Через це сервер перевищує RAM-ліміти та падає.

Питання:
Як організувати відправку такого файлу клієнту так, щоб памʼять майже не витрачалася?

Відповідь:
    Стратегія: потокова відправка (streaming response / chunked transfer encoding).

    Замість завантаження всього файлу у RAM перед відправкою — читати файл чанками
    (наприклад, по 64–256 KB) і віддавати їх клієнту поступово через генератор.
    У памʼяті одночасно знаходиться лише один чанк, тому споживання RAM не залежить
    від розміру файлу і залишається мінімальним (кілька сотень KB).

    У FastAPI це StreamingResponse з генератором, у Flask — Response з generator або
    send_file з підтримкою чанків, у Django — StreamingHttpResponse або FileResponse.
    HTTP-сервер автоматично використовує Transfer-Encoding: chunked.

    Інструменти: StreamingResponse (FastAPI/Starlette), StreamingHttpResponse (Django),
    генератор із file.read(chunk_size).
"""

"""
3. Обробка WAV-файлів (20–50 MB)

Сервіс отримує WAV-файли і виконує над ними обробку гучності та конвертацію.
Поточна реалізація робить кілька копій буфера під час кожної операції, що призводить до значного використання памʼяті.

Питання:
Як організувати обробку таких файлів, щоб уникнути дублювання великих буферів?

Відповідь:
    Стратегія: zero-copy обробка через memoryview + in-place мутація bytearray.

    Замість створення нових bytes-обʼєктів на кожному етапі обробки — зчитати WAV-дані
    в один bytearray і працювати з ним через memoryview. memoryview дозволяє створювати
    «зрізи»-вʼюшки на ті самі дані без копіювання, а bytearray — мутувати байти на місці.
    Таким чином, обробка гучності чи конвертація модифікує оригінальний буфер напряму,
    без проміжних копій, і памʼять використовується лише на один екземпляр файлу.

    Для числової обробки аудіосемплів також ефективний numpy з frombuffer() (zero-copy view
    поверх того ж bytearray), що дає vectorized-операції без додаткових алокацій.

    Інструменти: bytearray, memoryview, numpy.frombuffer(), struct.unpack_from().
"""

"""
4. Аналіз великого масиву чисел у API

API отримує масив числових значень (до 5 млн елементів) і має виконати математичні обчислення
(на кшталт згладжування чи пошуку піків).
Поточний Python-код працює довго й споживає багато памʼяті через створення проміжних структур.

Питання:
Який підхід дозволить пришвидшити обробку і зменшити витрати памʼяті?

Відповідь:
    Стратегія: C-розширення / векторизовані обчислення (vectorized computation) через NumPy.

    Чистий Python зберігає кожне число як окремий обʼєкт (~28 байтів на int, ~24 на float),
    а проміжні list comprehensions створюють мільйони тимчасових обʼєктів. NumPy натомість
    зберігає 5 млн чисел у суцільному C-масиві (наприклад, float64 — 8 байтів на елемент,
    ~40 MB замість ~200+ MB у Python-списку) і виконує операції поелементно на рівні C
    без створення проміжних Python-обʼєктів.

    Згладжування реалізується через numpy.convolve() або scipy.signal, пошук піків —
    через scipy.signal.find_peaks(). Усе це працює in-place або з мінімальними алокаціями,
    на порядок швидше за чистий Python і з набагато меншим споживанням RAM.

    Інструменти: numpy (ndarray, convolve, vectorized ops), scipy.signal, array.array (stdlib).
"""

"""
5. Парсинг лог-файлу 2–5 GB

Потрібно проаналізувати великий лог-файл у текстовому форматі: витягнути лише рядки, які містять певні ключові слова.
Файл повністю в оперативну памʼять завантажити неможливо.

Питання:
Який підхід дозволить зчитати файл максимально швидко та ефективно по памʼяті?

Відповідь:
    Стратегія: memory-mapped I/O (mmap) + потокова фільтрація.

    mmap відображає файл у віртуальний адресний простір процесу — ОС підвантажує
    лише ті сторінки, до яких відбувається доступ, тому фізична RAM використовується
    мінімально навіть для файлу 5 GB. Пошук ключових слів можна виконувати через
    mmap.find() або re.finditer() прямо по mmap-обʼєкту без декодування всього файлу.

    Альтернативний (простіший) підхід — потокове читання рядок за рядком через
    for line in open(path, 'rb'), що використовує буферизований I/O і тримає в RAM
    лише один рядок. Це простіше у реалізації, але mmap швидший, бо уникає
    копіювання даних з kernel-буфера у user-space і дозволяє ОС оптимізувати read-ahead.

    Інструменти: mmap.mmap(), open() з for-ітерацією, re для фільтрації.
"""

"""
6. Генерація великих HTML документів при високому RPS

Cервіс генерує HTML-документи обʼємом до 2 MB при навантаженні ~1000 RPS.
Використання конкатенації рядків створює багато тимчасових обʼєктів та призводить до фрагментації памʼяті.

Питання:
Який підхід дозволяє стабілізувати використання памʼяті та зменшити кількість алокацій?

Відповідь:
    Стратегія: буферизована збірка у bytearray / io.BytesIO + перевикористання буферів (object pooling).

    Конкатенація рядків (str + str) створює новий обʼєкт на кожну операцію — при документі 2 MB
    і тисячах фрагментів це означає тисячі тимчасових алокацій, що спричиняє фрагментацію
    і навантаження на GC. Замість цього слід записувати фрагменти у заздалегідь алокований
    bytearray або io.BytesIO — append у bytearray амортизовано O(1) і не створює проміжних обʼєктів.

    При ~1000 RPS додатково ефективно перевикористовувати буфери через пул обʼєктів:
    після відправки відповіді bytearray не звільняється, а повертається у пул, обнуляється
    (truncate/seek(0)) і використовується для наступного запиту. Це стабілізує кількість
    живих обʼєктів і практично усуває фрагментацію памʼяті.

    Інструменти: bytearray, io.BytesIO, queue.SimpleQueue (як пул буферів),
    ''.join() / b''.join() (якщо список фрагментів уже зібраний).
"""
